// Step 1: Declare the score
let score = 120; // You can change this to test different scores

// Step 2: Use the ternary operator to set the level
let level = (score > 100) ? 2 : 1;

// Step 3: Output the result
console.log(`Score: ${score}, Level: ${level}`);
